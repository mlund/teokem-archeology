# Roadmap for bulk.f90

- [ ] Fix runtime error
- [ ] Fix compiler warnings
- [ ] Loops can use simple names when used as index, e.g. "i", "j", "k", ...
- [ ] Make constants read-only w. `parameter`, e.g. pi, avogadros number etc and initialize directly in .inc file
- [ ] Migrate format labels to modern inline formatting
- [ ] If possible, reduce scope of global variables from bulk_f90.inc
- [ ] Can ran2.f90 be replaced by built in random number generator?
- [ ] Modernize and convert `examples/bulk/README` to markdown

